# CuTe DSL migration dispatch evidence

| Condition | Entry point | Route | Evidence | Reason |
| --------- | ----------- | ----- | -------- | ------ |
| Hopper/Blackwell, supported dtype, pack | `pack_sequence_fwdbwd` | CuTe DSL | 1.10-1.92x across measured widths | CuTe is faster in every measured pack case |
| Hopper/Blackwell, BF16/FP16 unpack | `unpack_sequence_fwdbwd` | CuTe DSL | 1.015-1.92x across measured BF16 widths | CuTe is non-regressing across the measured sweep |
| Hopper/Blackwell, FP32 unpack, width <8192 | `unpack_sequence_fwdbwd` | CuTe DSL | 1.11-1.91x through measured width 3000 | CuTe is faster in measured smaller-width regimes |
| Hopper/Blackwell, FP32 unpack, width >=8192 | `unpack_sequence_fwdbwd` | Triton baseline temporarily | CuTe 196.45 us vs Triton 195.46 us at width 8192 | Preserve the no-regression gate until NCU-guided store-path optimization closes the 0.5% gap |
| Hopper/Blackwell, dense vector local cumsum | `chunk_local_cumsum` | CuTe DSL | 1.95-2.08x for FP32/BF16 at measured T=63/500 | Per-chunk parallelism keeps the serial feature scan short and well occupied |
| Hopper/Blackwell, dense vector global cumsum, T <=64 | `chunk_global_cumsum` | CuTe DSL | 1.89-1.98x for FP32/BF16 at measured T=63 | Short scans remove Triton launch/autotune overhead |
| Hopper/Blackwell, dense vector global cumsum, T >64 | `chunk_global_cumsum` | Triton baseline temporarily | CuTe 0.43-0.47x for FP32/BF16 at measured T=500 | Preserve no-regression until the cooperative long-scan kernel is implemented |
| Hopper/Blackwell, ragged vector local cumsum | `chunk_local_cumsum` | CuTe DSL | 1.32-1.92x on measured FP16/FP32 ragged cases; 6/6 frozen tests and 8/8 extended oracle cases pass | Independent chunk work preserves parallelism across segment boundaries |
| Hopper/Blackwell, dense/ragged scalar local cumsum | `chunk_local_cumsum` | CuTe DSL | 1.11-2.39x dense and 1.86x ragged across measured FP32/BF16 cases | Width-one reuse retains independent chunk parallelism |
| Hopper/Blackwell, dense scalar global cumsum | `chunk_global_cumsum` | CuTe DSL cooperative scan | 1.71-1.98x for FP32/BF16 at measured T=63/500/2048; 20/20 frozen and 24/24 extended oracle cases pass | CTA-wide tiled scan maintains parallelism and carries exact FP32 state across tiles |
| Hopper/Blackwell, dense contiguous common fused recurrent forward, scalar/no gate, K/V <=64, T <=16 | `fused_recurrent_fwd` | CuTe DSL | 1.43-2.38x across 16 measured FP32/BF16 envelope cases; 6/6 forward/backward oracle configurations pass | CuTe removes Triton launch overhead for decode and short sequences while retaining FP32 recurrent state |
| Common fused recurrent backward or T >16/other gates/layouts/ragged shapes | `fused_recurrent_fwd`, `fused_recurrent_bwd` | Triton baseline | K=V=64 FP32 CuTe falls from 0.75x at T=64 to 0.43x at T=512 | Preserve no-regression until a tiled/warp-specialized long-sequence CuTe design replaces the register-heavy kernel |
| Hopper/Blackwell, eager contiguous CUDA int32/int64 cumulative lengths without CPU mirror | `prepare_position_ids` | CuTe DSL | 4.80-5.25x synchronized wall-time speedup across measured 1-4096 sequence cases; 24/24 frozen and edge cases pass | A single direct materialization kernel replaces repeat-interleave, cumsum, indexing, and their host synchronization |
| `torch.compile`, CPU mirror, CPU/NPU, or unsupported cumulative-length layout | `prepare_position_ids` | Existing PyTorch path | Frozen graph-count and CPU-mirror contracts pass | Preserve graph capture and the existing avoid-sync path |
| Hopper/Blackwell, contiguous CUDA FP16/BF16/FP32 beta logits, 65536 to int32-max elements | `fused_beta_sigmoid_fwd`, `fused_beta_sigmoid_bwd` | CuTe DSL | forward 1.72-1.80x and backward 1.55-1.66x across measured 65K-2.1M tensors; 39/39 direct/public oracle cases pass | Precise FP32 math with a lower-overhead TVM-FFI launch wins throughout the guarded envelope |
| Beta sigmoid tensors below 65536 elements or unsupported device/layout/dtype | `fused_beta_sigmoid_fwd`, `fused_beta_sigmoid_bwd` | Triton baseline | Explicit conservative guard | Keep the established route outside the fully integration-tested large-tensor envelope |
| Hopper/Blackwell, dense contiguous FP16/BF16/FP32 mean-pooling forward, chunk <=128, D <=512 | `mean_pooling_fwd` | CuTe DSL | 1.33-2.40x across measured frozen shapes; exact forward numerics | Feature-lane parallelism and a serial short-chunk reduction remove autotuned Triton overhead |
| Same dense mean-pooling backward, D <=1024 | `mean_pooling_bwd` | CuTe DSL | 1.13-2.50x across measured frozen shapes; exact gradients | Direct per-feature broadcast remains faster through the wider D=1024 case |
| Ragged mean pooling, forward D >512, or backward D >1024 | `mean_pooling_fwd`, `mean_pooling_bwd` | Triton baseline | CuTe forward is 0.63-0.90x at D=1024; frozen mixed-route case passes | Preserve no regression while a tiled wide/ragged kernel remains unimplemented |
| Converted CuTe consumer kernels requiring natural/base-2 softplus | `softplus_cute`, `softplus2_cute` | Native CuTe scalar primitive | bitwise exact against NVIDIA Triton PTX over 4112 edge/random inputs | Reuse the native approximate exp2/log2 implementation inside consumers; it is not a standalone launch or dispatch decision |
| Hopper/Blackwell, eager contiguous FP16/BF16/FP32 softmax rows, D<=1024 | `softmax_fwd`, `softmax_bwd` | CuTe DSL | forward 2.08-2.14x and backward 1.99-2.09x across measured rows; 18/18 oracle and frozen GSA integration pass | CTA reductions plus TVM-FFI launch beat every measured Triton width/row-count case |
| `torch.compile`, D>1024, unsupported device/layout/dtype | `softmax_fwd`, `softmax_bwd` | Triton baseline | Explicit verifier boundary | Preserve the existing path outside the measured native reduction envelope |
| Hopper/Blackwell, eager contiguous FP16/BF16/FP32 logsumexp, N>=128, 32<=D<=256, supported scale | `logsumexp_fwd` | CuTe DSL | 3.49-4.52x across 46 measured FP32/BF16 cases; 122/122 precise/fast oracle cases pass | Warp/shared native reduction eliminates Triton autotune and second-stage reduction overhead |
| Logsumexp non-power-of-two D with scale<=0 | `logsumexp_fwd` | Triton baseline | Triton observably returns NaN from scaled masked padding; fallback parity is tested | Preserve existing numerical behavior even though the mathematical reduction would be finite |
| Logsumexp N<128, D outside 32-256, compile, or unsupported dtype/layout/scale | `logsumexp_fwd` | Triton baseline | Explicit conservative envelope | Keep original behavior outside measured nonregressing shapes |
| Hopper/Blackwell, eager dense contiguous int64 block CSR with scan work <=64 | `prepare_block_csr` | CuTe DSL | 1.58-3.42x across measured scalar/tensor-count cases; exact offsets and per-row index multisets | Fuse count, prefix offsets, and deterministic scatter into one launch where the serial scan is genuinely tiny |
| Varlen, compile, nonconforming CSR inputs, or dense scan work >64 | `prepare_block_csr` | Triton baseline | 18/18 frozen dense/varlen cases pass; work=128 exposed a 0.92x CuTe case | Preserve no regression outside the measured launch-fusion envelope |
| Hopper/Blackwell, contiguous FP16/BF16/FP32 logcumsumexp with T=64-512, S=128-256, and 256-8192 lanes | `logcumsumexp_fwd` via `chunk_abc` | CuTe DSL cooperative scan | 1.11-3.99x across the measured envelope; 38/38 direct cases and ABC forward/backward differential pass | Warp prefix composition parallelizes time while bounded feature work avoids over-occupancy |
| Logcumsumexp outside the measured T/S/lane envelope, compile, or unsupported device/layout/dtype | `logcumsumexp_fwd` | Triton baseline | Serial CuTe candidate measured only 0.45-0.73x; high-lane cooperative cases also regress outside the cap | Preserve no regression and the original autotuned route outside the proven envelope |
| Converted CuTe consumers requiring exp/exp2/log/log2/tanh | `fla.ops.backends.cute.op` | Native CuTe scalar primitives | Precise/fast edge oracle passes against active Triton helpers; native fast tanh remains available where this Triton build lacks `fast_tanhf` | Inline the DSL's native scalar math and avoid meaningless Triton compatibility shims for MMA, gather, and descriptors |
| Hopper/Blackwell, eager contiguous FP16/BF16/FP32 matmul/addmm with N=2-4, K=16-256, 128-8192 outputs | `matmul`, `addmm` | CuTe DSL warp reduction | 1.05-2.27x across measured envelope; activation and addmm broadcast oracle cases pass | Avoid Triton's minimum-N tile redundancy for narrow outputs while preserving fused epilogues |
| Matmul N=1, outputs >8192, compile, unsupported layouts/dtypes/activations | `matmul`, `addmm` | Triton baseline | Single-column K=256 measured 0.82-0.98x and large-output cases fell to 0.58-0.98x | Preserve tensor-core throughput outside the proven narrow launch-bound regime |
| Hopper/Blackwell dense ungated single-chunk `chunk_h_parallel`, no final state, K/V<=64 | `chunk_fwd_h` | CuTe DSL | 1.51-1.68x through measured T64/KV64; h/state-dtype oracle cases pass | Directly materialize the sole entering state and eliminate the unused Triton reduction launch |
| Same route with final state and T*K*V<=8192 | `chunk_fwd_h` | CuTe DSL short recurrence | 1.23-1.91x across measured FP32/BF16 boundaries | Short FP32 per-state outer-product accumulation remains launch-bound inside the measured cap |
| Gated/ragged/multi-chunk parallel state or final-state work >8192 | `chunk_fwd_h` | Triton baseline | CuTe falls to 0.33-0.94x at final-state work 16384-262144 | Preserve tensor-core state accumulation outside the bounded route |
| Hopper/Blackwell dense ungated single-split state forward, T<=16, K/V<=64, no final state | split `chunk_fwd_h` | CuTe DSL | 1.55-1.68x across measured FP32/BF16 cases; state/tail oracle passes | Fuse sole-split initialization and avoid the unused reduction path |
| Same single-split route with final state and T*K*V<=8192 | split `chunk_fwd_h` | CuTe DSL short recurrence | 1.25-1.97x across measured boundary cases | Short scalar outer-product accumulation remains launch-bound inside the cap |
| Gated/ragged/multi-split state or final-state work >8192 | split `chunk_fwd_h` | Triton baseline | CuTe is 0.83-0.89x at work65536 | Retain tensor-core split/reduction kernels beyond the measured route |
| Hopper/Blackwell dense ungated FP32 fused chunk forward, T<=16, K/V<=64, T*K<=64 | `fused_chunk_fwd` | CuTe DSL | 1.16-1.27x across measured initial/final-state cases; differential and tail checks pass | A single short recurrence avoids Triton autotune and tensor-core setup overhead inside the measured FP32 envelope |
| Low-precision fused chunk or T*K>64 | `fused_chunk_fwd` | Triton baseline | BF16 includes 0.88-0.98x cases and FP32 T*K=128 is 0.98-0.99x | Preserve the no-regression boundary outside the proven FP32 short-work route |
| Hopper/Blackwell dense ungated K-first single-chunk `common.chunk_fwd_h` without final state | `chunk_fwd_h` | CuTe DSL | 1.06-1.18x across measured FP16/BF16/FP32 shapes; 25/25 oracle configurations pass | Direct state materialization removes the unused Triton reduction/autotune work |
| `common.chunk_fwd_h` with final state, gates, V-first state, ragged input, or multiple chunks | `chunk_fwd_h` | Triton baseline | Scalar final-state recurrence measured 0.46-1.04x | Preserve tensor-core accumulation and established general semantics outside the copy-only route |
| Hopper/Blackwell CP-local scalar-gated/ungated transfer, one sequence, T<=8, K16-32, V<=32 | `chunk_gated_delta_rule_fwd_h_pre_process` | CuTe DSL | 5.73-10.28x across 16 measured cases; h and M differential oracles pass | One launch builds both local transfer blocks and avoids the heavyweight autotuned matrix pipeline for tiny CP shards |
| CP DPLR/gk/multi-sequence/wider transfer preprocessing | `chunk_gated_delta_rule_fwd_h_pre_process` | Triton baseline | Explicit strict guard | Preserve the established general distributed preprocessing path outside the proven local envelope |
| Hopper/Blackwell dense FP32 BT32 triangular solve to FP16/BF16 with at least 2048 blocks | `solve_tril` | CuTe DSL native MMA | 1.62x at 2048 programs and 2.06x at 4096; independent inverse residual remains below 4e-3 | A single warp kernel replaces the multi-stage Triton solve/merge pipeline once enough blocks amortize setup |
| BT16/64, ragged, FP32 output, noncontiguous, or fewer than 2048 BT32 blocks | `solve_tril` | Triton baseline | CuTe measures 0.66-1.03x below the threshold | Preserve the established kernels throughout the non-winning envelope |
| Hopper/Blackwell dense ungated FP16/BF16 `chunk_fwd_o`, T=16 and K=V=16 | `chunk_fwd_o` | CuTe DSL native MMA | 1.16-1.17x; grouped-head and K/V-first state-layout oracles pass | A single warp covers the exact tensor-core tile and avoids Triton autotune overhead |
| `chunk_fwd_o` with larger K/V, gates, ragged input, or other lengths | `chunk_fwd_o` | Triton baseline | K/V32 measures 0.94x and K/V64 0.60x | Preserve the established tensor-core kernel outside the one-tile winning route |
| Hopper/Blackwell dense contiguous common recurrent forward with per-head constant decay, T<=16, K/V<=64 | `fused_recurrent_fwd` via recurrent RetNet/Lightning Attention | CuTe DSL | 1.41-2.37x across 9 FP16/BF16/FP32 cases; output/final-state max error 3.82e-6 | Reuse the short register-state recurrence while loading one constant decay per head |
| Hopper/Blackwell dense gated Oja forward, B=1, H=HV=4, T<=8, K=V=32, headwise beta | `fused_recurrent_gated_oja_rule` | CuTe DSL | 1.12-1.86x across FP16/BF16 T1-8; max differential error 1.19e-7 in the accepted envelope | Four independent warps keep each state column in registers and avoid Triton setup overhead |
| Other gated Oja shapes, vector beta, Q/K normalization, or ragged input | `fused_recurrent_gated_oja_rule` | Triton baseline | B2/HV8/T16 measured 0.87-0.88x and K64 measured 0.70x | Preserve the established kernel outside the exact winning shape family |
| Hopper/Blackwell dense contiguous FP32 HGRN chunk, T<=128 and B*D>=256 | `chunk_hgrn` | CuTe DSL | forward 2.30-3.33x and backward 4.48-7.24x; FP32 max error 5.73e-6 | One recurrence launch per direction removes chunk-prefix and fixup launch overhead |
| Low-precision HGRN chunk or shapes outside the measured envelope | `chunk_hgrn` | Triton baseline | Frozen FP16 B1/T63/D500 exceeded the public test's 0.005 tolerance despite passing the looser direct benchmark gate | Preserve the established low-precision rounding contract |
| Hopper/Blackwell contiguous RWKV4 recurrence, B=1, T<=16, C<=8192 | `fused_recurrent_rwkv4` | CuTe DSL | forward 1.94-2.35x and backward 1.98-2.08x across 12 stable FP16/BF16/FP32 cases | Fuse state concatenation in forward and eliminate batch reductions in backward |
| RWKV4 with B>1, T>16, unsupported layout/dtype, or wider channels | `fused_recurrent_rwkv4` | Triton baseline | Explicit measured-envelope guard | Preserve established vectorized recurrence outside the launch-bound route |
| Hopper/Blackwell dense contiguous common recurrent forward with exactly one K-axis or V-axis decay, T<=16, K/V<=64 | `fused_recurrent_fwd` via recurrent GLA/GSA consumers | CuTe DSL | gk 1.18-2.38x and gv 1.46-2.38x across accepted FP16/BF16/FP32 cases; max error 3.82e-6 | Keep axis-specific decay inside the same register-state kernel and avoid Triton launch/autotune overhead |
| FP32 K-axis recurrent decay with K>32 | `fused_recurrent_fwd` | Triton baseline | CuTe is only 1.02x at K64 | Require a meaningful margin rather than routing a statistically marginal case |
| Hopper/Blackwell contiguous FP16/BF16 REBASED forward, K=V=16 and T<=16 | `parallel_rebased` | CuTe DSL native MMA | 2.00-3.38x across 8 exact raw/public forward and unchanged-backward cases | A single native-MMA warp removes partial-buffer and reduction launches for the one-tile regime |
| Hopper/Blackwell dense pre-activated GDN2 decode, T=1, K=V=32, H>=2, at least four value-head programs, K-first state | `fused_recurrent_gdn2_fwd` | CuTe DSL | 1.10-1.66x across 8 retained FP16/BF16 batch/head cases; outputs and final states are exact | A value lane keeps one complete state column in registers and fuses decay, erase, write, and query work |
| GDN2 longer sequences, K/V64, V-first state, fused gate activation, normalization, packed/continuous batching, or unsupported layouts | `fused_recurrent_gdn2_fwd` | Triton baseline | Broad CuTe cases measure 0.33-0.43x at T8-16 and 0.92-0.95x for V-first T1 | Preserve the established recurrent kernel outside the exact nonregressing decode slice |
| Hopper/Blackwell dense contiguous FP16/BF16 parallel retention, T=K=V=16 and B*H>=8, without attention materialization | `parallel_retention` | CuTe DSL native MMA | 1.59-1.69x across 24 forward/backward batch/head/dtype cases with strict Triton parity | A single warp fuses the decayed causal score tile and value product while reusing the established backward |
| Parallel retention with other shapes, packed sequences, attention output, unsupported layouts/dtypes, or fewer than eight head programs | `parallel_retention` | Simple GLA Triton baseline | Explicit exact-envelope guard | Preserve the general implementation and occupancy outside the measured native-MMA tile |
| Hopper/Blackwell dense FP16/BF16 PATH inference decode, T=1, K=V=64, B*HQ>=4 | `parallel_path_attn` | CuTe DSL | 16.69-22.72x across 16 MHA/GQA gated/ungated cases; output and optional key cache are exact | Fuse the multi-stage one-token transform and attention graph into one CTA per query head |
| PATH training, T>1, varlen, other dimensions, unsupported layouts/dtypes, or fewer than four query-head programs | `parallel_path_attn` | Triton baseline | T8 candidate cache differed in 71.5% of elements; frozen long/varlen shapes remain outside the guard | Preserve the established transformed-cache and backward contracts beyond exact one-token decode |
